<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Champion Calendar - Página Principal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Fondo de la página */
        body, html {
            height: 100%;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(to right, #f4791f, #659999);
            color: white;
        }

        /* Contenedor del título */
        .hero-container {
            text-align: center;
            padding: 50px;
        }

        /* Título principal */
        .hero-title {
            font-size: 3rem;
            font-weight: bold;
            animation: fadeIn 2s ease-in-out;
        }

        /* Botón de entrada */
        .btn-enter {
            padding: 15px 30px;
            font-size: 1.25rem;
            background-color: #007bff;
            border: none;
            border-radius: 30px;
            color: white;
            transition: transform 0.3s ease;
        }

        .btn-enter:hover {
            background-color: #0056b3;
            transform: scale(1.1);
        }

        /* Animación para el título */
        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(-20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Estilos de la imagen de fondo */
        .background-image {
            background-image: url('/imagenes/portada2.webp');
            background-size: cover;
            background-position: center;
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            opacity: 0.6;
        }
    </style>
</head>
<body>

    <!-- Imagen de fondo -->
    <div class="background-image"></div>

    <div class="hero-container">
        <h1 class="hero-title">Bienvenido a Champion Calendar</h1>
        <p class="lead">¡Tu lugar para seguir los equipos y jugadores más importantes del mundo!</p>

        <!-- Botón para entrar -->
        <a href="/equipos" class="btn btn-enter">Entrar a los Equipos</a>
    </div>

    <!-- Bootstrap JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
