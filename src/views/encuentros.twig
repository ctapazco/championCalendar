<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Encuentros - Champion Calendar</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">
    <h1 class="text-center">Lista de Encuentros</h1>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>Local</th>
                <th>Visitante</th>
                <th>Fecha</th>
                <th>Hora</th>
                <th>Resultado</th>
            </tr>
        </thead>
        <tbody>
            {% for encuentro in encuentros %}
                <tr>
                    <td>{{ encuentro.id }}</td>
                    <td>{{ encuentro.local }}</td>
                    <td>{{ encuentro.visitante }}</td>
                    <td>{{ encuentro.fecha }}</td>
                    <td>{{ encuentro.hora }}</td>
                    <td>{{ encuentro.resultado_local }} - {{ encuentro.resultado_visitante }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
