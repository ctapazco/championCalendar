<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jugadores - Champion Calendar</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">
    <h1 class="text-center mb-4">Lista de Jugadores</h1>

    <table class="table table-bordered table-striped text-center">
        <thead class="table-dark">
            <tr>
                <th>Nombre</th>
                <th>Edad</th>
                <th>Dorsal</th>
                <th>Equipo</th>
                <th>Goles</th>
                <th>Asistencias</th>
            </tr>
        </thead>
        <tbody>
            {% for jugador in jugadores %}
                <tr>
                    <td>{{ jugador.nombre }}</td>
                    <td>{{ jugador.edad }}</td>
                    <td>{{ jugador.dorsal }}</td>
                    <td>{{ jugador.equipo_id }}</td>
                    <td>{{ jugador.goles }}</td>
                    <td>{{ jugador.asistencias }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <h2 class="text-center mt-5">Agregar Nuevo Jugador</h2>
    <form action="/jugadores" method="POST" class="mt-3">
        <div class="mb-3">
            <label class="form-label">Nombre del Jugador</label>
            <input type="text" name="nombre" class="form-control" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Edad</label>
            <input type="number" name="edad" class="form-control" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Dorsal</label>
            <input type="number" name="dorsal" class="form-control" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Equipo ID</label>
            <input type="number" name="equipo_id" class="form-control" required>
        </div>
        <button type="submit" class="btn btn-primary">Agregar Jugador</button>
    </form>
</body>
</html>
